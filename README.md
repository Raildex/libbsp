### BSPLIB
Simple Library to consume Quake 1 BSP map files.  
This library is currently under active development for private usage.  
It gets new features as they needed
## Compilation
Just use xmake to build the library: `xmake build`